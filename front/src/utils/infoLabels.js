/**
 * Как называются типы аналитики в интерфейсе.
 *
 * Счёт хранит не название аналитики, а её тип (`info_1_type` и далее), поэтому
 * подпись поля выводится отсюда: у А100 первый слот — «Касса/Счёт», второй —
 * «Статья ДДС», и человек видит их, а не «аналитика 1» и «аналитика 2».
 */
export const INFO_LABELS = {
  partner:    'Контрагент',
  product:    'Номенклатура',
  department: 'Склад/Отдел',
  cash:       'Касса/Счёт',
  flow:       'Статья ДДС',
  expenses:   'Статья расхода',
  revenue:    'Статья дохода',
  employee:   'Сотрудник',
}

export default INFO_LABELS
